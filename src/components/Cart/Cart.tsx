import iconCart from '/images/icon-cart.svg';


export const Cart = () => {
    return (
        <div className="cart">
             <img src={iconCart} alt="cart" className="cart-img" />
        </div>
    )
}